# ppm.yml
<% 
  require 'chorus/tmuxinator'
  project = Chorus::Tmuxinator.new(path: path, name: 'ppm')
  project.root = project.root.join('repos/gems')
%>

name: <%= project.name %>
root: <%= project.root %>

# Global settings
attach: true
tmux_command: tmux

windows:
#   - ppm:
#       root: <%= project.subdir('packages') %>
#       layout: <%= project.layout(:claude) %>
#       panes:
#         -
  - pim:
      root: <%= project.subdir('pim') %>
      layout: <%= project.layout(:claude) %>
      panes:
        -
  - pcs:
      root: <%= project.subdir('pcs') %>
      layout: <%= project.layout(:claude) %>
      panes:
        -
        -
        -
  - opcred:
      root: <%= project.subdir('opcred') %>
      layout: <%= project.layout(:claude) %>
      panes:
        -
        - 
        -
  - chorus:
      root: <%= project.subdir('chorus') %>
      layout: <%= project.layout(:claude) %>
      panes:
        -
        -
        -
